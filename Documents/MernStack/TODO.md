# TODO List for Ethical Hacking Tutorial Web App

- [x] Initialize the project structure: Create 'client' folder for React app and 'server' folder for Express backend.
- [x] Set up React frontend: Use Create React App in 'client', install necessary dependencies (e.g., react-router-dom for routing), create components for navigation, tutorial sections, and individual tutorial pages.
- [x] Develop tutorial content: Create static content for tutorials (beginner: 5 topics, intermediate: 5, advanced: 5), including text, code snippets, and placeholders for images. Use free images via URLs.
- [ ] Set up Express backend: In 'server', create a basic Express server to serve the built React app (for MERN compliance, even if mostly static). Optionally, add MongoDB integration if dynamic features like user progress tracking are added later.
- [ ] Integrate frontend and backend: Configure the server to serve the React build, add CORS if needed.
- [ ] Add styling and resources: Use CSS or a library like Bootstrap for responsive design. Add a free favicon.
- [ ] Testing: Run the app locally to ensure tutorials load and navigation works.
- [ ] Deployment preparation: Build the React app, create a GitHub repository, push code, and set up GitHub Pages for the frontend.
